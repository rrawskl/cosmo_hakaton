# Проверки версии 2.2.0

19 сентября 2026: аудит формул и источников описан в [MODEL_AUDIT](MODEL_AUDIT.md).
119 backend-тестов, 4 Vitest и 8 Playwright прошли; production build,
TypeScript и Ruff успешны. Новые проверки: неполный набор механизмов,
группа лучших окон, единственный победитель, невозможные значения прогнозов,
повторные интервалы, будущий Issued, отмена и варианты срока предупреждений.

На перезапущенной версии 2.2.0 проверены живые current и historical_replay:
JSON совпал со snapshot, PDF получен, offline reproduce matches=true.
SHA-256 шести публичных raw текущего расчёта и двух исторического совпали
с сохранёнными доказательствами. Историческое сравнение проверено визуально:
реальные разные диапазоны Kp видны рядом с равными длительностями критериев.
Два контрольных исторических эксперимента повторены; их масштаб не позволяет
заявлять доказанную общую точность прогноза. Ниже сохранены прежние протоколы.

## Проверки версии 2.1.0

## Повторная полная проверка 19 сентября 2026, около 10:32 UTC

- Backend: 103 passed, два предупреждения о deprecated API зависимостей.
- Vitest: 4 passed. Playwright Chromium: 8 passed (18.2 с), включая
  живой NOAA, состояния ошибок/устаревания, replay, навигацию и мобильный экран.
- Next.js production build, проверка TypeScript и Ruff: успешно.
- БД: health=ok, версия 2.1.0; Alembic 0001 (head).
- NOAA 6h/1d/3d/7d: OK, последний отсчёт 10:20 UTC на момент проверки.
- Живой current с горизонтом 0: single_window/attention; с горизонтом
  12 ч: equal/attention. Historical replay и review для 10.05.2024:
  equal/attention. Во всех четырёх случаях орбита присутствует,
  reproduce matches=true, JSON совпадает с результатом, PDF корректно отдаётся.
- Контрольные исторические эксперименты повторены: один hit и один
  correct negative; выборка из двух случаев не подтверждает общую точность.
- Новых ошибок в проверенных сценариях не найдено. Это проверка локального
  приложения, а не гарантия доступности внешних источников или точности прогноза.
  Docker/PostgreSQL runtime и публичное развёртывание не проверены: Docker
  и целевой сервер отсутствуют. Архив измерений GOES за 2024 не подключён.

## Предыдущий протокол 2.1.0

Дополнение: 103 backend-теста прошли после изменения прогноза окна.
Проверены S1/cutoff, исключение stale-прогноза, оба канала наблюдений,
объединение предупреждений, необязательная орбита, частичный результат,
резервный источник и offline reproduce с орбитой и без неё.
- Frontend: 4 Vitest и 8 Playwright-сценариев прошли; production build успешен.
- Живая проверка 19 сентября 2026: текущее окно и историческое окно
  10 мая 2024 получили «Требует внимания», три альтернативы равнозначны.
  Текущие наблюдения GOES отдельно показали NORMAL; они не экстраполируются.
- Для обоих анализов JSON совпал со snapshot, offline reproduce: matches=true.
  PDF экспортирован, первая страница визуально проверена.
- Орбита рассчитана из пригодного кеша с отметкой устаревшего получения;
  резервный Space-Track GP проверен живым запросом для NORAD 25544.
Ниже сохранён протокол предыдущей версии 2.0.0.

- Backend: 92 tests passed; два предупреждения зависимостей Starlette/AnyIO.
- Пороговые пары 9.999/10, 99.999/100, 999.999/1000, 9999.999/10000,
  99999.999/100000 проверены; отдельный ≥100 MeV ≥1 pfu проверен.
- Проверены три энергии, ordering, duplicates/conflicts, units, NaN/null,
  отрицательные значения, malformed JSON, future/stale, rising/falling/stable,
  timeout/retry, primary failure, secondary fallback, both unavailable, cache,
  window coverage, отсутствие прогноза из наблюдений и двойного учёта S1.
- Regression: SGP4 Vallado, исторический cutoff, JSON/PDF, сохранение,
  независимая тестовая БД, закрытый admin endpoint и отказ источников.
- Frontend: 4 Vitest. Playwright: 8 сценариев; состояния NORMAL/S1/S3, stale/unavailable,
  slow/error API, live NOAA без mocks, исторический replay и export.
  Размеры: 1920, 1440, 1366, 768, 390px; горизонтального overflow нет.
- Next production build/TypeScript проходит.
- Живой /protons/current: HTTP200, primary GOES-18, observed_at
  2026-09-19T05:20:00Z, ≥10=0.2496662586927414; ≥50=0.2298649549484253;
  ≥100=0.2284029722213745 pfu, BELOW_S1/NORMAL. Это зафиксированная проверка,
  не встроенные рабочие значения. NOAA ответ показан в браузерном графике.
- Все четыре живых диапазона 6h/1d/3d/7d ответили HTTP200/OK.
- Новый current-анализ содержит space_weather/protons/illumination.
  JSON равен snapshot; POST reproduce matches=true без сети; PDF создан.
- Реальные исторические орбиты проверены ранее на 1 мая, 10 мая, 20 июня 2024;
  исторический replay повторно проверяется браузерным regression-тестом.
- Эксперимент NOAA Kp на двух исторических эпизодах сохранён в
  data/experiments/results.json; это не оценка точности протонного прогноза.

## Ограничения

Протонный rolling API не содержит 2024 год. Для него DATA_UNAVAILABLE.
Наблюдения GOES не прогнозируют будущие часы ВКД и не измеряют дозу на МКС.
В версии 2.1.0 будущие окна оцениваются по официальному S1-прогнозу. Уверенного
выбора может не быть при пробелах в прогнозе; доступны отдельные выводы.
NOAA/CelesTrak могут быть недоступны; состояние видно пользователю.
Docker Desktop отсутствует: Compose/PostgreSQL runtime не проверен.
Внешний сервер/домен не предоставлен, публикация веб-приложения не выполнялась.
